apply plugin: 'com.noveogroup.android.check'

check {
    checkstyle {
        config file("$rootDir/config/checkstyle/checkstyle.xml")
        abortOnError true
    }
    findbugs { skip true }
    pmd { skip true }
}